require 'spec_helper'

describe RailsEventStoreDynamoid do
  it 'has a version number' do
    expect(RailsEventStoreDynamoid::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
